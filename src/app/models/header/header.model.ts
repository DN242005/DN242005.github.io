export interface Header {
  id?: string;
  name: string;
  email: string;
  phoneNumber: string;
  goalLife: string;
  photoUrl: string;
  location: string;
  social: string;
  // social?: string; ← si vas a usar este campo también, agrégalo aquí
}
